import React from 'react'
// import PropTypes from 'prop-types'
import BaseWords from './style'

/**
 * Words
 * @component
 *
 */
function Words(props) {
  return <BaseWords {...props} />
}
Words.defaultProps = {}
Words.propTypes = {}

export default Words
